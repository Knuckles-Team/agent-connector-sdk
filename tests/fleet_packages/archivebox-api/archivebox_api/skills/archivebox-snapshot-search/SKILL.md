---
name: archivebox-snapshot-search
description: Browse archived snapshots through the archivebox-api MCP server.
license: MIT
---
# ArchiveBox snapshot search

Call `archivebox_core` with `action=get_snapshots` and page with `page` and `limit`.
