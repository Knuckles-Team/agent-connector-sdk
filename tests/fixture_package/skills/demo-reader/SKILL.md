---
name: demo-reader
description: Read the demo stream one page at a time through the demo-agent MCP server.
license: MIT
---
# Demo reader

Call `demo_reader` with `action=stream_contents` and follow `continuation`.
